"""Tree algorithm modules.

Each module exports:
  * a tree class (annotated with spytial decorators)
  * `def trace(keys: list[int]) -> list[tuple[str, Tree]]`

Class definitions are copied verbatim from `spytial-clrs/src/trees.ipynb`.
"""
