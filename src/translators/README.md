## Translators


Translators take CnD `InstanceLayout` objects and translate them to 
constraints in a particular language.

Because of this, translators do **not** implement a specific
interface (they are too tied to their specific context).