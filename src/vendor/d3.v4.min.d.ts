// Type declarations for the vendored D3 v4 runtime (d3.v4.min.js).
// Adjacent to d3.v4.min.js so `import('./vendor/d3.v4.min.js')` in index.ts
// type-resolves. Loose by design — the module is only stashed on `window.d3`.
declare const d3: any;
export default d3;
